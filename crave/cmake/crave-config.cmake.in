@PACKAGE_INIT@
include(CMakeFindDependencyMacro)

set(metaSMT_DIR "${CMAKE_INSTALL_LIBDIR}/cmake")
find_dependency(metaSMT)

if(NOT TARGET crave::crave)
    include("${CMAKE_CURRENT_LIST_DIR}/crave-targets.cmake")
    add_library(crave::crave ALIAS crave)
endif()

set(crave_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set(crave_lIBRARIES crave::crave)