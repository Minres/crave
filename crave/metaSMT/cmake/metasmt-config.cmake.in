@PACKAGE_INIT@
include(CMakeFindDependencyMacro)

find_package(cudd REQUIRED)
find_package(z3 REQUIRED)
find_package(Boolector)
find_package(CVC4)
find_package(yices2)
find_package(aiger)
find_package(picosat)
find_package(minisat)
find_package(lingeling)
find_package(STP)

if(NOT TARGET metaSMT::metaSMT)
    include("${CMAKE_CURRENT_LIST_DIR}/metasmt-targets.cmake")
    add_library(metaSMT::metaSMT ALIAS metaSMT)
endif()

set(metaSMT_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set(metaSMT_lIBRARIES metaSMT::metaSMT)