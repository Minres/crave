@PACKAGE_INIT@
include(CMakeFindDependencyMacro)

if(NOT TARGET aiger)
    include("${CMAKE_CURRENT_LIST_DIR}/aiger-targets.cmake")
    add_library(aiger::aiger ALIAS aiger)
endif()

set(aiger_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set(aiger_lIBRARIES aiger::aiger)