@PACKAGE_INIT@
include(CMakeFindDependencyMacro)

if(NOT TARGET gmp::gmp)
    include("${CMAKE_CURRENT_LIST_DIR}/gmp-targets.cmake")
    add_library(gmp::gmp ALIAS gmp)
endif()

set(gmp_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set(gmp_lIBRARIES gmp::gmp)
